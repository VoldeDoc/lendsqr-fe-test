version: '3.8'

services:
  lendsqr-app:
    build: .
    ports:
      - "$PORT:3000"  # Railway's PORT to container port 3000
    environment:
      - NODE_ENV=production
      - PORT=$PORT
    command: ["/app/start.sh"]