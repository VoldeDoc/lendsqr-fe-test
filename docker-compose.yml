version: '3.8'

services:
  lendsqr-app:
    build: .
    ports:
      - "${PORT:-3000}:3000"  # Use PORT env var or default to 3000
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3000}
    command: ["/app/start.sh"]