version: '3.8'

services:
  lendsqr-app:
    build: .
    ports:
      - "3000:3000"  # Only expose frontend port
    environment:
      - NODE_ENV=production
      - VITE_BASE_URL=http://localhost:5000  # Internal API URL
    command: ["/app/start.sh"]